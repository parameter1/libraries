const connectionProjection = require('./connection-projection');
const getProjection = require('./get-projection');
const typeProjection = require('./type-projection');

module.exports = {
  connectionProjection,
  getProjection,
  typeProjection,
};
